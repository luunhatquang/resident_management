from django.apps import AppConfig

class NotificationConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'resident_manage.apps.notification'
    verbose_name = 'notification'