from django.apps import AppConfig

class ContractConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'resident_management.apps.contract'
    verbose_name = 'contract'